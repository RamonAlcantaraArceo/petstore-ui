/**
 * Visual reporter package bootstrap entrypoint.
 *
 * Side effect: imports and executes the visual report mount code, which loads
 * report data and renders the visual reporter UI into the DOM.
 */
import '../../../src/visual-report/bootstrap';
