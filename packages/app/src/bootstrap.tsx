/**
 * App package bootstrap entrypoint.
 *
 * Side effect: imports and executes the petstore app mount code, which renders
 * the React application into the `#root` DOM container.
 */
import './petstore/index';
