{
  "name": "@petstore-ui/app",
  "version": "0.0.0",
  "private": true,
  "description": "Petstore application package (migration scaffold)",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "pnpm --dir ../.. exec vitest run packages/app",
    "lint": "pnpm --dir ../.. exec eslint packages/app --ext .ts,.tsx",
    "format": "pnpm --dir ../.. exec prettier --write packages/app",
    "format:check": "pnpm --dir ../.. exec prettier --check packages/app",
    "type-check": "tsc --noEmit -p ../../tsconfig.json"
  }
}
